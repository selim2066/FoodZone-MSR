import React from 'react';
import Navbar from '../../../component/Navbar/Navbar';
import DisplayCart from '../../../component/Navbar/DisplayCart';

const Cart = () => {
    return (
        <div>
            <Navbar/>
            <DisplayCart/>
        </div>
    );
};

export default Cart;